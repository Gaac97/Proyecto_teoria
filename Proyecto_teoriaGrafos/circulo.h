#ifndef CIRCULO_H
#define CIRCULO_H
#include <vector>
#include <SFML/Graphics.hpp>

class circulo
{
    public:
        circulo(int p);
        //listaArista * aristas;
        int pos;
        virtual ~circulo();

    protected:

    private:
};

#endif // CIRCULO_H
